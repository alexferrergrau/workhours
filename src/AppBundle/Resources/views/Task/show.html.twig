{% extends '::base.html.twig' %}

{% block body -%}

<div class="container-fluid">
    <div class="row">
        <div class="form col-xs-12 col-md-10">
    <h2>Tarea: {{ entity.title }}</h2>
    <h4>Descripción: {{ entity.description }}</h4>
    <h4>Horas: {{ entity.hours }}</h4>



        <ul class="record_actions">
    <li>
        <a href="{{ path('task') }}"><button type="button" class="btn btn-primary pull-left">
            Volver al listado
                </button>
        </a>
    </li>
    <li>
        <a href="{{ path('task_edit', { 'id': entity.id }) }}"><button type="button" class="btn btn-warning pull-left">
            Editar tarea
            </button>
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>

        </div>
    </div>
</div>
{% endblock %}
